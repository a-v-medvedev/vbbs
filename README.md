# vbbs
Very basic batch system

```
git clone --branch v1.x https://github.com/fpagliughi/sockpp.git
cd sockpp
mkdir build
cd build/
cmake -DSOCKPP_BUILD_STATIC=ON -DCMAKE_INSTALL_PREFIX=../../sockpp.bin ..
make -j
make install
cd ../..
make
cp vbbs vbbx_client vbbs_server /path/with/binaries
```
